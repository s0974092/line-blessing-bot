export interface Theme {
  id: string;
  name: string;
  defaultText: string;
  prompt: string;
  thumbnail: string;
}

export interface Style {
  id: string;
  name: string;
  prompt: string;
  thumbnail: string;
}
