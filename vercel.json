{
  "installCommand": "npm install --omit=dev",
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "env": {
    "LD_LIBRARY_PATH": "/var/task/lib:$LD_LIBRARY_PATH"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}