# ==================================
# LINE Bot Settings (Required)
# ==================================
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# ==================================
# Service API Keys (Required)
# ==================================
# Google Gemini API Key
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Max retry attempts for AI generation
#GEMINI_GENERATION_MAX_ATTEMPTS=3

# The fallback prompt template for generating generic blessing text. Use {minLength}, {maxLength} as placeholders.
#GEMINI_FALLBACK_PROMPT_TEMPLATE=請生成一句長度介於{minLength}到{maxLength}個字之間的通用中文祝福語。請直接提供祝福語文字，不要包含任何其他說明或引號。

# The final hardcoded fallback text if all AI generation fails
#GEMINI_FINAL_FALLBACK_TEXT=平安喜樂，萬事如意

# Cloudinary Credentials
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ==================================
# Infrastructure (Required)
# ==================================
# Redis connection URL for session state
REDIS_URL=redis://localhost:6379

# ==================================
# Bot Behavior (Optional)
# ==================================
# All variables below have default values in the code.
# Uncomment and set them to override the defaults.

# --- High Priority (Styling & Core Logic) ---
# The delay in milliseconds before deleting an image from Cloudinary after sending
#IMAGE_DELETION_DELAY_MS=10000

# The main prompt template for generating blessing text. Use {theme}, {style}, {minLength}, {maxLength} as placeholders.
#GEMINI_BLESSING_PROMPT_TEMPLATE=請根據主題「{theme}」和風格「{style}」，生成一句長度介於{minLength}到{maxLength}個字之間的繁體中文祝福語。請直接提供祝福語文字，不要包含任何其他說明或引號。

# Min/Max length for the generated blessing text
#GEMINI_TEXT_MIN_LENGTH=5
#GEMINI_TEXT_MAX_LENGTH=15

# Divisor for calculating font size based on image width (smaller number = larger font)
#IMAGE_FONT_SIZE_RATIO_DIVISOR=20

# RGBA color for the text background (e.g., 'rgba(0, 0, 0, 0.5)')
#IMAGE_BG_COLOR_RGBA=rgba(0, 0, 0, 0.5)

# HEX color for the text itself (e.g., '#FFFFFF')
#IMAGE_TEXT_COLOR_HEX=#FFFFFF


# --- Lower Priority (UI Text & Other Logic) ---
# The keywords that trigger the image generation flow (comma-separated)
#BOT_TRIGGER_KEYWORDS=開始,生成圖片,長輩圖

# The welcome message sent to new users or when using a trigger keyword
#WELCOME_MESSAGE=哈囉！我是您的專屬祝福圖片生成器！🌸\n您可以透過我輕鬆生成帶有祝福語的圖片，並分享給親朋好友。\n請輸入 {keywords} 來製作您的第一張長輩圖吧！

# The error message shown when image generation fails
#GENERATION_ERROR_MESSAGE=圖片生成失敗，系統有點忙，請稍後再試一次。

# The maximum character length for the user's custom blessing text
#BOT_MAX_TEXT_LENGTH=20

# The session/state timeout in seconds
#USER_STATE_TTL_SECONDS=300
